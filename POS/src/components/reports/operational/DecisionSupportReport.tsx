import React from "react"

export default function DecisionSupportReport() {
	/*
	 * Recommendations:
	 *
	 * Reorder
	 * Reduce inventory
	 * Review pricing
	 * Promote product
	 * Investigate margin leakage
	 * Review supplier
	 * Investigate anomalies
	 *
	 * Every recommendation must include:
	 * evidence + confidence + expected impact.
	 */

	return <section data-report="decision-support" />
}
