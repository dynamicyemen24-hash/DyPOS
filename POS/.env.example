# DyPOS Frontend Configuration
VITE_DYPOS_BACKEND=rest
VITE_DYPOS_API=http://localhost:3001/api
# Router base: "/" for standalone (Workers/PWA at domain root),
# "/pos" when embedded as the Frappe desk page. Auto-detected if unset.
# VITE_ROUTER_BASE=/