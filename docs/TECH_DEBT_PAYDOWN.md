# 🧾 سجل سداد الديون التقنية والوظيفية والعملياتية
**الحملة**: DyPOS Debt Paydown v1.0 — بمنهجية «أدلة أولًا، ثم سداد مُركّز، ثم تحقق بالبناء»
**التاريخ**: 2026-09-13

---

## ✅ ديون مسددة في هذه الحملة

### 1️⃣ الديون الوظيفية (Functional) — الأخطر على الإيرادات

| الدين | الدليل | السداد |
|-------|--------|--------|
| **تفتت منسّق العملة**: 3 تنفيذات متنافسة (`utils/currency.js` الكنوني، `helpers.js`، `toFixed(2)` خام) وأكثر من 17 غلافًا محليًا | `formatCurrency` مكرر في 17+ مكوّنًا | إضافة `formatCurrencySafe()` الكنونية الآمنة (null/string/NaN) في `utils/currency.js` |
| **شاشة البيع تتجاهل العملة والدقة**: `POSSale.vue` كانت تعرض `toFixed(2)` بلا رمز عملة ودقة ثابتة — تناقض مع باقي الشاشات | سطر 2550 سابقًا | توحيد على `formatCurrencySafe(amount, shiftStore.profileCurrency)` |
| **إيصالات الطباعة بتجاهل دقة النظام**: `printInvoice.js` كانت `toFixed(2)` صارمة | سطر 16 سابقًا | توحيد على `formatCurrencyNumber` (دقة `initPrecision` + إعدادات Dycos) |
| **كوبونات بمنسّق Intl مستقل**: `CouponManagement.vue` كانت تبني `Intl.NumberFormat` خاصًا | سطر 1132 سابقًا | توحيد على `formatCurrencySafe` |
| **`useFormatters.js` منسّق منفصل**: كان `toFixed(2)` ثابتًا | — | تفويض كامل إلى `utils/currency.js` — مصدر حقيقة واحد |
| **علة أسبقية مؤثرات (Real Bug)**: `currencyCode \|\| settingsStore.pos_profile ? "USD" : "Unknown"` — الأقواس الضائعة تجعلها دائمًا USD | `utils/helpers.js:111` | الملف حُذف كله (كود ميت — صفر مستوردات) |

> **القاعدة الجديدة**: أي منسّق عملة جديد يمر عبر `utils/currency.js` — لا `toFixed` ولا `Intl` مباشر.

### 2️⃣ الديون التقنية (Technical)

| الدين | الدليل | السداد |
|-------|--------|--------|
| **28 `console.log` صامتة** تتسرب للإنتاج وتتجاهل منظومة Logger المركزية | مسح شامل | سداد 26 موضعًا في 7 ملفات (`csrf`, `apiWrapper`, `useInvoice`, `useOffline`, `customerSearch`, `PartialPayments`) → `log.debug` / `log.error`. (موضعان في `usePOSEvents.js` هما أمثلة JSDoc — سليمان) |
| **كود ميت**: `utils/helpers.js` بلا أي مستورد + يحمل علّة أسبقية | مسح الاستيراد | حذف الملف |
| **حِزم ضخمة بلا تجزئة بائع**: index 772KB + POSSale 720KB، وأي نشر كود يبطل كاش كل شيء | `vite.config.js` | `manualChunks`: `vendor-vue` (vue/@vue/vue-router/pinia) + `vendor-offline` (dexie/idb) — كاش طويل الأمد للإطارات |
| **دين الظلال القديم** (`!important` للحوارات) | `index.css` | حُفظ كـ legacy موثق داخل نظام التصميم v1.0 (لا يُمس حتى هجرة الحوارات) |

### 3️⃣ الديون العملياتية (Operational)

| الدين | الدليل | السداد |
|-------|--------|--------|
| **مخلفات بناء في المستودع**: 4 ملفات `vite.config.js.timestamp-*.mjs` | جذر `POS/` | حذف + قاعدة `*.timestamp-*.mjs` في `.gitignore` |
| **سجل نشر متسرب**: `deploy.log` | جذر المستودع | حذف (مغطى أصلًا بـ `*.log`) |

---

## 📋 سجل الديون المتبقية (Backlog مُرتّب بالأولوية)

| # | الدين | الأثر | الأولوية | خطة السداد |
|---|-------|-------|----------|------------|
| 1 | `PaymentDialog.vue` — 3324 سطرًا (أكبر ملف) | صيانة صعبة، إختبار شبه مستحيل | 🔴 عالية | استخراج `usePaymentSplit` + `usePaymentValidation` كوحدات قابلة للاختبار |
| 2 | `POSSale.vue` — 2845 سطرًا | نقطة تعارض دائمة | 🔴 عالية | استخراج `useSaleShortcuts` و`useSessionLock` |
| 3 | أغلفة `formatCurrency` المحلية في 15 مكوّنًا (مكررة لكن صحيحة) | ضجيج تكرار | 🟡 متوسطة | استبدال تدريجي بـ `formatCurrencySafe` مع كل تعديل قريب |
| 4 | `itemSearch.js` — 2116 سطرًا في store واحد | أداء فهرسة الكتالوج الكبير | 🟡 متوسطة | توثيق موجود جزئيًا في `docs/LARGE_CATALOG_OPTIMIZATION.md` |
| 5 | `z-index` legacy في `index.css` (`!important` × 4) | هشاشة طبقات | 🟢 منخفضة | الهجرة إلى `--dy-z-*` عند توحيد الحوارات |
| ~~6~~ | ~~حجم صورة الشعار 1MB~~ | — | ✅ **مسدد** | 1254px→512px: 1048KB←324KB (الأصل محفوظ في `imag/`) |

**قاعدة السداد المستقبلية**: كل PR يمس ملفًا من الجدول أعلاه يسدد بندًا واحدًا على الأقل منه — «قاعدة المخيم: اتركه أنظف مما وجدته».

---

## 🏆 جولة الجودة القياسية (World-Class Round) — مكتملة

### 1) حماية منطق المال بالاختبارات (كانت صفرًا)
- `vitest.config.js` + `tests/currency.test.js` — **22 اختبارًا تغطي**: التقريب المصرفي والتجاري (تعادلات .5 الحقيقية مثل `2.125→2.12`)، دقة العملة/الطفو من إعدادات ERPNext، `formatCurrencySafe` ضد القيم الفاسدة، رموز العملات الخليجية
- ⚠️ درس الاختبارات نفسه: التقريب لمنزلتين لا يحرك `2.5` — التعادل الحقيقي عند المنزلة التالية (`2.125`)

### 2) التحميل الكسول الذكي — أثر مقاس على POSSale
- **20 مكوّنًا ثقيلًا** (حوارات + إدارة) حُوّلت إلى `defineAsyncComponent`
- **`POSSale.js`: 720.38 KB ← 316.84 KB (−56%)**
- حزمة الدفع (الأكثر استخدامًا) تُجلب مسبقًا في وقت الخمول (`requestIdleCallback`) لتظل فورية
- ظهرت حِزم مستقلة: `PaymentDialog` 65KB • `PromotionManagement` 54KB • `InvoiceManagement` 45KB • `ReturnInvoiceDialog` 43KB

### 3) بوابة جودة تمنع ارتداد الديون
- `biome.json`: `noConsole: error` (مسموح فقط `warn`/`error`، وإعفاء لـ `logger.js` و`scripts/`)
- أمر موحد: **`npm run verify`** = اختبارات + lint
- آخر سجل حي: **صفر** `console.log` متبقية في الكود الحي (البقية داخل Logger ذاته وأمثلة JSDoc)

### 4) تحصين هندسي للبناء
- `outDir` صار **مطلقًا** (`path.resolve(import.meta.dirname, ...)`) في موضعيه — انحراف الإخراج مع اختلاف دليل التشغيل أصبح مستحيلًا بنائيًا
- ملاحظة ترحيل: هدف النشر الحالي هو تطبيق `DyPOS` المستقل (`../DyPOS/public/pos` + `www/`) — جميع الفحوص أعلاه مؤكدة عليه

---

## 🛡️ جولة التحصين الشامل (Hardening Round) — مكتملة

### 1) علة إنتاج حقيقية اصطادتها الاختبارات
- **`parseError(null)` كان يُسقط الواجهة بانهيار كامل** (TypeError في معالج الأخطاء نفسه = شاشة معلقة للكاشير) — أُصلح في المصدر بحماية `error ?? {}`، مع اختبار حارس دائم

### 2) شبكة الاختبارات: 43 ← **84 اختبارًا (100% نجاح)**
| الملف | يحمي |
|-------|------|
| `currency.test.js` (22) | منطق المال والتقريب المطابق لـ frappe |
| `arabic.test.js` (21) | التطبيع العربي للبحث |
| `network.test.js` (26) | withTimeout، تصنيف الأخطاء، retryAsync بالتراجع الأُسّي |
| `memoize.test.js` (6) | دمج نداءات البحث المتزامنة (عواصف النقر) |
| `errorHandler.test.js` (16) | رسائل الأخطاء التي يراها الكاشير (frappe كامل) |

### 3) بوابة CI رسمية
- وظيفة `frontend` جديدة في `.github/workflows/ci.yml`: تثبيت ← `npm run verify` ← بناء — الجودة صارت **شرط دمج** لا توصية

### 4) ضغط أصول الهوية
- الشعار: 1254×1254 (1048KB) ← 512×512 (324KB) — **−70%**، الأصل محفوظ في `imag/DyPOSLogo-original-1254.png`
- **PWA precache: 5926KB ← 5196KB (−730KB)** — تحميل أوفلاين أول أسرع لكل كاشير
